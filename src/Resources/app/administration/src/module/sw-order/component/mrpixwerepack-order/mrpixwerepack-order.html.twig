<div v-if="weRepackOrder">
    <sw-container columns="1fr 1fr">
        <div>
            <sw-description-list class="mrpix-werepack-list">
                <dt>WeRePack</dt>
                <dd v-if="weRepackOrder.isRepack">
                    <mt-icon name="regular-shopping-bag" color="#88fc03"></mt-icon>
                    {{ $tc('mrpixwerepack.order.weRepackSelected') }}
                </dd>
                <dd v-else>
                    <mt-icon name="regular-trash" color="#fc3903"></mt-icon>
                    {{ $tc('mrpixwerepack.order.weRepackNotSelected') }}
                </dd>

                <dt>{{ $tc('mrpixwerepack.order.voucher') }}</dt>
                <dd v-if="weRepackOrder.promotionIndividualCode">
                    <router-link
                            :to="{ name: 'sw.promotion.v2.detail', params: { id: weRepackOrder.promotionIndividualCode.promotionId } }">
                        {{ weRepackOrder.promotionIndividualCode.code }}
                    </router-link>
                </dd>
                <dd v-else>{{ $tc('mrpixwerepack.order.noVoucher') }}</dd>
            </sw-description-list>
        </div>
    </sw-container>
</div>
<div v-else>
    <strong>{{ $tc('mrpixwerepack.order.notAvailable') }}</strong>
</div>